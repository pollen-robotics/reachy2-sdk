# Live viewing and recording with ReRun

## Installation

Extra packages can be installed with

```shell
pip install -e .[examples]
```

## Run

```shell
python src/examples/rerun/rerun_recorder.py --urdf_path <path_to_reachy.urdf>
```
